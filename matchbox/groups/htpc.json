{
  "name": "htpc",
  "profile": "debian-installer",
  "selector": {
    "mac": "${__HTPC_MAC}"
  },
  "metadata": {
    "boot": {
      "hostname": "${__HTPC_HOSTNAME}"
    },
    "playbook_repo": "${__playbook_repo}",
    "proxy": {"http":"${__http_proxy}"}
  }
}
