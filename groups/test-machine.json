{
  "name": "test",
  "profile": "debian-installer",
  "selector": {
    "mac": "${__TEST_MAC}"
  },
  "metadata": {
    "boot": {
      "hostname": "${__TEST_HOSTNAME}"
    },
    "proxy": {"http":"${__http_proxy}"}
  }
}
